struct node {
    struct node * next;
    struct node * prev;
    int * value;
};

void add_value(struct node ** valuep, int *v) {

    struct node ** v = malloc (sizeof **v);
    struct node *val = malloc(sizeof *val);
}